
export async function checkAccessFolder() {
  // const path = import.meta.env.DEV ? "videos" : "Videos"
  // const response = await fetch(`${baseUrl}discover/drive?folderPath=public/${path}`)
  // const { access } = await response.json()
  // return access ? 1 : 0
}